# Benchmark: California Housing

**Description:** Predict median house value from block group features — regression, ~20.6K rows, all numeric.
**Source:** [sklearn datasets](https://scikit-learn.org/stable/modules/generated/sklearn.datasets.fetch_california_housing.html)
**Date:** 2026-05-14T01:13:48.557270+00:00
**Runs averaged:** 3

---

## Dataset Overview

| Property | Value |
|----------|-------|
| Task Type | regression |
| Total Rows | 20,640 |
| Training Rows | 16,512 |
| Test Rows (hold-out) | 4,128 |
| Kept (training) | 10,586 |
| Reduction | 35.89% |
| Features (aligned) | 8 |
| Target Column | MedHouseVal |
| Classes | 3675 |
| Optimization Runtime | 312.0658s |
| Total Pipeline | 313.0732s |

### Integrity Check

- Structure preserved: True
- Target preserved: True
- Columns preserved: True
- Class distribution maintained: True

**Safety Adjustments:** class_preservation_applied

---

## Model Performance Comparison

*Both models trained on training set (80%), evaluated on hold-out test set (20%).*

### Random Forest

| Metric | Original | Pruned | Δ |
|--------|----------|--------|---|
| R2 | 0.8047 | 0.7945 | -0.0102 |
| MSE | 0.2559 | 0.2693 | -0.0134 |
| MAE | 0.3284 | 0.3431 | -0.0147 |
| Training Time (s) | 3.8905 | 2.2533 | 1.6372 |

### XGBoost

| Metric | Original | Pruned | Δ |
|--------|----------|--------|---|
| R2 | 0.8309 | 0.8257 | -0.0053 |
| MSE | 0.2215 | 0.2285 | -0.0069 |
| MAE | 0.3095 | 0.3222 | -0.0127 |
| Training Time (s) | 0.2246 | 0.1588 | 0.0658 |

---

## Methodology (Leak-Free)

**Critical: This benchmark uses a SPLIT-FIRST design to prevent train/test leakage.**

1. **Dataset loaded** as pandas DataFrame
2. **Target detection**: manual — column `MedHouseVal`
3. **Train/test split**: 80/20 stratified split (seed 42) BEFORE any pruning
4. **Column reordering**: target moved to last column (standard DataPruning convention)
5. **Preprocessing**: standardized preprocessing and feature alignment applied independently to train and test sets
6. **Feature alignment**: train/test feature spaces are intersected to ensure consistent dimensionality for model evaluation
7. **Dataset optimization**: the DataPruning production optimization pipeline processes the **TRAINING SET ONLY** — the test set is NEVER touched by optimization logic
8. **Safety layer**: reduction cap and class preservation applied to training set only
9. **Model training**: models are trained on the original training set AND the optimized training set
10. **Evaluation**: BOTH models are evaluated on the SAME untouched test set — the only variable is the training data

### Why This Matters

If the optimization used the full dataset (including test rows), the test evaluation would be invalid —
the model would appear to perform better simply because the test set was cherry-picked.
By splitting FIRST and optimizing ONLY the training set, we ensure:
- The test set represents genuinely unseen data
- Any improvement (or preservation) of metrics is due to better training data quality
- Results are scientifically defensible for ML peer review

---

## Reproducibility

To reproduce this benchmark:

1. Clone the [DataPruning Benchmark repository](https://github.com/DataPruning/benchmarks)
2. Install dependencies: `pip install -r benchmark_runner/requirements.txt`
3. Run: `python -m benchmark_runner.run --name california_housing --runs 3`

Benchmarks are generated using the **same production optimization standards and evaluation methodology** used by [DataPruning](https://datapruning.com).

### Fixed Parameters

| Parameter | Value |
|-----------|-------|
| Train/Test Split | 0.2 |
| Random Seed | 42 |
| Stratified Split | False |
| Optimization Profile | Production |
| Feature Alignment | aligned feature spaces (train/test) |

### Dataset Licensing

This repository does **not** redistribute original datasets. Users must:

1. Download the dataset from the original source (linked above)
2. Use the provided `download.py` script
3. Verify licensing terms before redistributing any data files

The optimized dataset output is generated through the DataPruning pipeline.
Users should consult the original dataset license regarding derivative works.

### Model Stack (Deterministic)

All benchmarks use the same fixed model stack:

**Classification:** Logistic Regression, Random Forest, XGBoost
**Regression:** Random Forest, XGBoost

Same hyperparameters, same random seed, same train/test split across ALL benchmarks.
Results are directly comparable across datasets.

---

*Generated by DataPruning Benchmark Runner on 2026-05-14T01:13:48.557270+00:00*